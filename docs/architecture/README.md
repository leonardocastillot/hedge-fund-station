# Architecture Docs

This folder is for system shape, runtime contracts, and scaling decisions.

Start here:

- `../project-architecture.md` for the repo-wide architecture contract
- `../hedge-fund-agent-operating-model.md` for backend-first hedge fund rules
- `../hyperliquid-strategy-roadmap.md` for the strategy build sequence
- `../backend-runtime-contract.md` for backend runtime expectations
- `backend-source-of-truth.md` for frontend service ownership boundaries
- `mac-app-store-gap-analysis.md` for Mac App Store readiness gaps

Compatibility note: existing architecture docs may still live at the root of
`docs/` during the incremental cleanup. Prefer adding new architecture docs here.
